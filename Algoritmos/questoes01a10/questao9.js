let readline = require('readline-sync');

// Leia 2 números (A, B) e escreva-os em ordem inversa (B, A).

//Entrada

let A = Number(readline.question('Digite um número: '));
let B = Number(readline.question('Digite outro número: '));

//Processamento

//Saída

console.log(`Os números na ordem inversa são ${B} , ${A}`);